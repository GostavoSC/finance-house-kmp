CREATE TABLE Payments(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    description TEXT NOT NULL,
    date TEXT NOT NULL,
    value REAL NOT NULL
);


selectAllPayments:
SELECT * FROM Payments;

insertPayment:
INSERT INTO Payments(id, name, description, date, value) VALUES (?,?,?,?,?);
